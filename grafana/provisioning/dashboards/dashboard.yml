apiVersion: 1

providers:
  - name: default
    folder: Payment API
    type: file
    options:
      path: /var/lib/grafana/dashboards/payment-api

  - name: infrastructure
    folder: Infrastructure
    type: file
    options:
      path: /var/lib/grafana/dashboards/infrastructure
